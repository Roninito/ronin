# See [`../ARCHITECTURE.md`](../ARCHITECTURE.md)

That file at the repo root is canonical. This file is intentionally a stub —
an earlier pass let a second, independent copy live here and it drifted out
of sync with the root document. Don't recreate that: update the root file,
not this one.
